## 1. 工程基线复制

- [x] 1.1 盘点 `../HtmlParser` 中需要复用的工程基线文件与目录，排除 HTML 领域实现与无关产物
- [x] 1.2 将选定的包元数据、构建配置、测试配置、格式化配置、忽略规则和发布辅助文件复制到当前仓库
- [x] 1.3 创建 `src` 最小源码入口与必要目录骨架，确保工程结构可被现有工具链识别

## 2. 项目身份重写

- [x] 2.1 更新 `package.json` 中的包名、描述、入口、导出和发布字段为 `@hamster-note/txt-parser`
- [x] 2.2 批量校正 README、构建配置、测试配置和发布辅助文件中的项目命名与输出路径
- [x] 2.3 复查仓库内残留的 `HtmlParser`、`html-parser` 和 HTML 语义文案并完成清理

## 3. 开发链路对齐

- [x] 3.1 对齐 TypeScript、Rolldown、Jest、ESLint、Prettier 等工具链配置并保持本地独立可用
- [x] 3.2 补齐 `build`、`typecheck`、`test` 等标准脚本或等效命令入口
- [x] 3.3 确认初始化后的工程不依赖 `yarn link`、软链接或跨仓库配置继承

## 4. 验证与收尾

- [x] 4.1 安装依赖并执行类型检查、测试和构建，确认工作区具备最小可验证开发链路
- [x] 4.2 根据验证结果裁剪明显与 HTML 强绑定且不属于工程基线的依赖或文件
- [x] 4.3 更新变更说明与必要文档，记录当前初始化范围及后续待补事项
